#include "stdafx.h"
#include "Pattern.h"

const string Pattern::tableName = "Pattern";

const string Pattern::keyName = "id";

const array<string, 3> Pattern::fieldNames = { "id", "textColor", "backColor" };